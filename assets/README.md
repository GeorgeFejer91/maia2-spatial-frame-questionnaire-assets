# Assets

This folder contains participant-facing media used by the questionnaire program.

## Pictographs

- `pictographs/spatial-frame-reference-continuum.png`
  - Use in Block 2 and Block 3.
  - Choices: `A`, `B`, `C`, `D`, `E`, `F`, `G`, `H`.
  - The image itself is language-neutral because it uses letter labels. Localize the prompt and buttons outside the image.

Keep image geometry unchanged unless the study team explicitly approves a new stimulus version. If a visual variant is created, record the new filename, checksum, date, reason, and whether it remains comparable to the original.
